import { html, LitElement } from "lit";

const componentName = "dw-chip-select";

export class DwChipSelect extends LitElement {
  render() {
    return html`Dw Chip Select`;
  }
}

customElements.define(componentName, DwChipSelect);
